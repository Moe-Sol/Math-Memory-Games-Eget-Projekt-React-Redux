
const initialState = {
};


const secondReducer = (state = initialState, action) => {
    // switch (action.type) {
        // case 'sameCards':
        //     if (state.allSameCards.indexOf(action.data) === -1) {
        //         console.log(state.allSameCards)
        //         return {
        //             allSameCards: [...state.allSameCards, action.data]
        //         }
        //     }

        //     else { return state }



        // default:
            return state
    // }
}

export default secondReducer;


